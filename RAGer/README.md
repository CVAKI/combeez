# RAGer

Drop your colleague's RAGer module source here (any .py/.ts/.rs/etc files,
in any folder structure). Combeez Monitor scans this folder live and shows
it in the RAGer explorer tab with real syntax highlighting and a real file tree.
